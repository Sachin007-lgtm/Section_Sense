import { useState } from 'react';
import Index from './Index';

// This is just a wrapper to keep the existing search functionality
const Search = () => {
  return <Index />;
};

export default Search;
